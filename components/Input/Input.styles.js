import {StyleSheet} from 'react-native';

export default styles = StyleSheet.create({
    container: {
        flexDirection:'row',
        padding:5,
        margin:10,
        borderBottomColor:"white",
        borderBottomWidth:2

    },

    input: {
        flex:1,
        fontSize:20,
        fontWeight:'bold',
        color:'white'
    }
})